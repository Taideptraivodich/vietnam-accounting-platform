# INT-01A Failure Reporting Format

This file mirrors `FAILURE_REPORTING_FORMAT.md` for compatibility with the original dispatch naming.

Every failure is reported in `openIssues[]` with:

```text
step
title
error.name
error.message
error.details
```

A failure in any mandatory INT-01A step blocks Final Integration Gate v1.0.3.
